#include <iostream>
#include <string>

std::string readWord(std::istream& input) {
    std::string word;
    input >> word;
    return word;
}
int main() {
    auto execute = true;
    while (execute) {
        
    }
    return 0;
}
